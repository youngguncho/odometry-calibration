package irp_sen;

struct cpc_t {
    int64_t utime;       // timestamp

    int32_t n;           // number of points

    float x[n];          // points
    float y[n];
    float z[n];
    color_float_t colors[n];
}
