package hauv;

struct pl_gbp_t
{
    int64_t time;
    double x;
    double y;
    double relative_bearing; // removed by Bluefin; we keep it for old log file compatibility
    boolean is_depth;
    double depth_altitude;
}
